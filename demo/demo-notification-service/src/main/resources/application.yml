server:
  port: 8086

spring:
  application:
    name: notification-service

monitoring:
  server-url: ${MONITORING_SERVER_URL:http://localhost:8080}
  actuator-url: ${MONITORING_ACTUATOR_URL:http://localhost:8086/actuator}
  base-url: ${MONITORING_BASE_URL:http://localhost:8086}
