server:
  port: 8081

spring:
  application:
    name: inventory-service

# Override the monitoring server URL if different from annotation default (localhost:8080)
# monitoring:
#   server-url: http://monitoring-server:8080
